diff --git a/index.html b/index.html
index a5a29a2..4ee57c7 100644
--- a/index.html
+++ b/index.html
@@ -786,8 +786,6 @@ body { background:#edf0e9; } #home-view { padding:clamp(24px,6vw,80px) 28px; ove
                 dfCurrentDriftPts += points;
                 // Bank drift score every physics step so the HUD cannot fall at drift end.
                 dfScore += points;
-                // Bank drift score every physics step so the HUD cannot fall at drift end.
-                dfScore += points;
                 awardDriftCoins(points);
                 
                 dfSkids.push({x: leftW.x, y: leftW.y, life: 1.0});
@@ -812,8 +810,6 @@ body { background:#edf0e9; } #home-view { padding:clamp(24px,6vw,80px) 28px; ove
                     dfCurrentDriftPts = 0;
                     // A new slide starts a fresh 100-point coin counter.
                     dfDriftCoinProgress = 0;
-                    // A new slide starts a fresh 100-point coin counter.
-                    dfDriftCoinProgress = 0;
                 }
                 if (Math.abs(dfCar.speed) > 6 && driftAngle > 0.15 && Math.random() > 0.5) {
                     dfSkids.push({x: leftW.x, y: leftW.y, life: 0.3});
@@ -1552,7 +1548,7 @@ body { background:#edf0e9; } #home-view { padding:clamp(24px,6vw,80px) 28px; ove
         let zmPlayer, zmZombies, zmBullets, zmScore, zmFrame, zmBuildings, zmPowerups, camera, activePowerup;
         
 function initZombies() {
-    zmPlayer={x:CITY_START,y:CITY_START,r:15,speed:4}; zmScore=0;zmFrame=0;
+    zmPlayer={x:CITY_START,y:CITY_START,r:15,speed:4,hp:20,maxHp:20,hitCooldown:0}; zmScore=0;zmFrame=0;
     zmZombies=[];zmBullets=[];zmPowerups=[];zmBuildings=[];
     activePowerup={type:'none',timer:0};raidShotTick=-20;raidFlash=0;raidFiring=false;
     for(let row=0;row<CITY_BLOCKS;row++)for(let col=0;col<CITY_BLOCKS;col++) {
@@ -1584,6 +1580,17 @@ function shoot() {
     for(const angle of angles)zmBullets.push({x:zmPlayer.x,y:zmPlayer.y,vx:Math.cos(angle)*15,vy:Math.sin(angle)*15,r:gun.radius?5:4,damage:gun.damage,blast:gun.radius,color:gun.color});
 }
 
+function callRaiders(){
+    if(raidRaidersCalled||zmScore<200)return;raidRaidersCalled=true;raidAircraft={x:zmPlayer.x-520,y:zmPlayer.y-390,life:420};raidAnnounce('RAIDERS INCOMING · CHINOOK DROPPING TEN HOSTILES');
+    for(let i=0;i<10;i++){const a=i*Math.PI*2/10,x=zmPlayer.x+Math.cos(a)*330,y=zmPlayer.y+Math.sin(a)*330;if(!zmBuildings.some(b=>rectIntersect(x,y,18,b)))zmZombies.push({x,y,r:18,hp:7,maxHp:7,speed:2.15,isRaider:true,phase:i*1.7,shots:5,reload:0,fire:35,strafe:i%2?1:-1});}
+}
+function updateRaider(z){
+    if(z.reload>0){z.reload--;if(z.reload===0)z.shots=5;return;}
+    const d=Math.hypot(zmPlayer.x-z.x,zmPlayer.y-z.y),a=Math.atan2(zmPlayer.y-z.y,zmPlayer.x-z.x),move=d>260?1:d<170?-1:0;
+    const x=z.x+Math.cos(a)*z.speed*move+Math.cos(a+Math.PI/2)*z.strafe*.7,y=z.y+Math.sin(a)*z.speed*move+Math.sin(a+Math.PI/2)*z.strafe*.7;
+    if(!zmBuildings.some(b=>rectIntersect(x,y,z.r,b))){z.x=x;z.y=y;}
+    if(--z.fire<=0&&d<540){z.fire=22+Math.floor(Math.random()*18);raidEnemyBullets.push({x:z.x,y:z.y,vx:Math.cos(a)*11,vy:Math.sin(a)*11,life:55});if(--z.shots<=0)z.reload=75;}
+}
 function updateZombies() {
     zmFrame++;if(raidFlash>0)raidFlash--;if(raidFiring)shoot();if(activePowerup.timer>0)activePowerup.timer--;
     for(const fx of raidExplosions)fx.life--;raidExplosions=raidExplosions.filter(fx=>fx.life>0);
@@ -1593,9 +1600,10 @@ function updateZombies() {
     if(!zmBuildings.some(b=>rectIntersect(px,zmPlayer.y,zmPlayer.r,b)))zmPlayer.x=px;
     if(!zmBuildings.some(b=>rectIntersect(zmPlayer.x,py,zmPlayer.r,b)))zmPlayer.y=py;
     camera.x=Math.max(0,Math.min(WORLD_SIZE-canvas.width,zmPlayer.x-canvas.width/2));camera.y=Math.max(0,Math.min(WORLD_SIZE-canvas.height,zmPlayer.y-canvas.height/2));
-    for(const chest of raidChests)if(!chest.opened&&Math.hypot(zmPlayer.x-chest.x,zmPlayer.y-chest.y)<38){chest.opened=true;raidWeapon=chest.weapon;raidShotTick=-30;raidAnnounce('CHEST OPENED · '+RAID_WEAPONS[raidWeapon].name+' equipped');}
-    for(let i=zmPowerups.length-1;i>=0;i--){const p=zmPowerups[i];if(Math.hypot(zmPlayer.x-p.x,zmPlayer.y-p.y)>=zmPlayer.r+15)continue;if(p.type==='nuke'){for(const z of zmZombies)raidDamage(z,z.isBoss?30:z.hp);raidExplosions.push({x:zmPlayer.x,y:zmPlayer.y,r:300,life:25,maxLife:25});}else activePowerup={type:p.type,timer:300};zmPowerups.splice(i,1);}
-    if(zmFrame%Math.max(15,50-Math.floor(zmScore/15))===0)spawnCityZombie(false);
+    for(const chest of raidChests)if(!chest.opened&&Math.hypot(zmPlayer.x-chest.x,zmPlayer.y-chest.y)<38){chest.opened=true;if(chest.bandage){raidBandages=Math.min(5,raidBandages+1);raidAnnounce('CHEST OPENED · BANDAGE FOUND · PRESS H');}else{raidUnlockedWeapons.add(chest.weapon);raidWeapon=chest.weapon;raidShotTick=-30;raidAnnounce('CHEST OPENED · '+RAID_WEAPONS[raidWeapon].name+' unlocked');}}
+    if(raidRadio&&!raidRadio.carried&&Math.hypot(zmPlayer.x-raidRadio.x,zmPlayer.y-raidRadio.y)<38){raidRadio.carried=true;raidAnnounce('RADIO SECURED · PRESS R TO CALL ASSISTANCE');}
+    for(let i=zmPowerups.length-1;i>=0;i--){const p=zmPowerups[i];if(Math.hypot(zmPlayer.x-p.x,zmPlayer.y-p.y)>=zmPlayer.r+15)continue;if(p.type==='nuke'){for(const z of zmZombies)if(!z.isRaider)raidDamage(z,z.isBoss?30:z.hp);raidExplosions.push({x:zmPlayer.x,y:zmPlayer.y,r:300,life:25,maxLife:25});}else activePowerup={type:p.type,timer:300};zmPowerups.splice(i,1);}
+    const spawnGap=zmScore>=200?95:Math.max(15,50-Math.floor(zmScore/15));if(zmFrame%spawnGap===0)spawnCityZombie(false);
     for(let i=zmBullets.length-1;i>=0;i--) {
         const b=zmBullets[i];let hit=false;
         // Substeps keep fast projectiles from skipping thin wrecks or small enemies.
@@ -1610,16 +1618,29 @@ function updateZombies() {
         if(hit)zmBullets.splice(i,1);
     }
     zmZombies=zmZombies.filter(z=>z.hp>0);
-    breakRaidLab();
+    breakRaidLab();callRaiders();
     spawnRaidReinforcements();
+    if(raidAircraft&&--raidAircraft.life<=0)raidAircraft=null;
+    if(raidSupport){
+        if(raidSupport.drop>0)raidSupport.drop--;
+        else {
+            const target=zmZombies.filter(z=>!z.isRaider&&z.hp>0).sort((a,b)=>Math.hypot(a.x-raidSupport.x,a.y-raidSupport.y)-Math.hypot(b.x-raidSupport.x,b.y-raidSupport.y))[0];
+            if(target&&Math.hypot(target.x-raidSupport.x,target.y-raidSupport.y)<440&&--raidSupport.shot<=0){raidSupport.shot=78;raidSupport.target=target;raidSupport.flash=4;raidDamage(target,1);}
+            if(raidSupport.flash>0)raidSupport.flash--;
+        }
+    }
+    if(zmPlayer.hitCooldown>0)zmPlayer.hitCooldown--;
+    for(let i=raidEnemyBullets.length-1;i>=0;i--){const b=raidEnemyBullets[i];b.x+=b.vx;b.y+=b.vy;if(--b.life<=0||zmBuildings.some(ob=>rectIntersect(b.x,b.y,2,ob))){raidEnemyBullets.splice(i,1);continue;}if(Math.hypot(b.x-zmPlayer.x,b.y-zmPlayer.y)<zmPlayer.r+3){if(!zmPlayer.hitCooldown){zmPlayer.hp-=4;zmPlayer.hitCooldown=35;}raidEnemyBullets.splice(i,1);}}
     for(const z of zmZombies) {
+        if(z.isRaider){updateRaider(z);continue;}
         const a=z.isLabBoss?bossDirection(z):Math.atan2(zmPlayer.y-z.y,zmPlayer.x-z.x);
-        const x=z.x+Math.cos(a)*z.speed,y=z.y+Math.sin(a)*z.speed;
+        const zSpeed=z.speed*(raidAfterRaid?1.2:1),x=z.x+Math.cos(a)*zSpeed,y=z.y+Math.sin(a)*zSpeed;
         if(!zmBuildings.some(b=>rectIntersect(x,y,z.r,b))){z.x=x;z.y=y;}
         else if(!zmBuildings.some(b=>rectIntersect(x,z.y,z.r,b)))z.x=x;
         else if(!zmBuildings.some(b=>rectIntersect(z.x,y,z.r,b)))z.y=y;
-        if(Math.hypot(z.x-zmPlayer.x,z.y-zmPlayer.y)<z.r+zmPlayer.r){gameOver(zmScore,'The city claimed you.');return;}
+        if(Math.hypot(z.x-zmPlayer.x,z.y-zmPlayer.y)<z.r+zmPlayer.r&& !zmPlayer.hitCooldown){zmPlayer.hp-=raidAfterRaid?5:4;zmPlayer.hitCooldown=35;}
     }
+    if(zmPlayer.hp<=0){gameOver(zmScore,'The city claimed you.');return;}
 }
 
 function drawZombies() {
@@ -1983,15 +2004,16 @@ const RAID_WEAPONS={
     heavy:{name:'Heavy Hitter',cooldown:13,damage:4,color:'#e4a7a1',radius:0}
 };
 let raidWeapon='starter',raidChests=[],raidExplosions=[],raidNotice='',raidNoticeUntil=0;
+let raidUnlockedWeapons=new Set(['starter']),raidEnemyBullets=[],raidRaidersCalled=false,raidAircraft=null,raidBandages=0,raidArmor=0,raidRadio=null,raidSupport=null,raidAfterRaid=false;
 let raidLab=null,raidLabBroken=false,raidBossDefeated=false,raidBossGrid=null,raidReinforcements=0;
 function initRaidExpansion() {
-    raidWeapon='starter';raidChests=[];raidExplosions=[];raidLabBroken=false;raidBossDefeated=false;raidBossGrid=null;raidReinforcements=0;raidNotice='';raidNoticeUntil=0;
+    raidWeapon='starter';raidUnlockedWeapons=new Set(['starter']);raidChests=[];raidExplosions=[];raidEnemyBullets=[];raidRaidersCalled=false;raidAircraft=null;raidBandages=0;raidArmor=0;raidRadio=null;raidSupport=null;raidAfterRaid=false;raidLabBroken=false;raidBossDefeated=false;raidBossGrid=null;raidReinforcements=0;raidNotice='';raidNoticeUntil=0;
     raidLab=zmBuildings.find(b=>b.kind==='building'&&b.x===CITY_START-150&&b.y===CITY_START-550);
     raidLab.isLab=true;
     for(let i=0;i<10;i++)for(let attempt=0;attempt<200;attempt++) {
         const x=90+Math.random()*(WORLD_SIZE-180),y=90+Math.random()*(WORLD_SIZE-180);
         if(Math.hypot(x-zmPlayer.x,y-zmPlayer.y)<220||zmBuildings.some(b=>rectIntersect(x,y,25,b))||raidChests.some(c=>Math.hypot(x-c.x,y-c.y)<350))continue;
-        raidChests.push({x,y,weapon:['speedshooter','boom','heavy'][Math.floor(Math.random()*3)],opened:false});break;
+        const bandage=Math.random()<.38;raidChests.push({x,y,weapon:bandage?null:['speedshooter','boom','heavy'][Math.floor(Math.random()*3)],bandage,opened:false});break;
     }
 }
 function raidAnnounce(message) {raidNotice=message;raidNoticeUntil=zmFrame+300;}
@@ -2028,12 +2050,13 @@ function raidDamage(z,amount) {
     z.hp-=amount;
     if(z.hp>0)return;
     zmScore+=z.isBoss?10:1;
+    if(z.isRaider){raidArmor++;zmPlayer.maxHp+=8;zmPlayer.hp=Math.min(zmPlayer.maxHp,zmPlayer.hp+4);raidBandages=Math.min(5,raidBandages+1);if(!zmZombies.some(other=>other!==z&&other.isRaider&&other.hp>0)){raidRadio={x:z.x,y:z.y};raidAfterRaid=true;raidAnnounce('LAST RAIDER DOWN · RADIO DROPPED · PRESS R TO CALL HELP');}else raidAnnounce('RAIDER DOWN · ARMOR PLATE +8 MAX HP · BANDAGE FOUND');}
     if(z.isFirstLabBoss&&!raidBossDefeated){
         raidBossDefeated=true;
         for(const enemy of zmZombies)if(!enemy.isBoss&&enemy.hp>0){enemy.hp+=1;enemy.maxHp=(enemy.maxHp||1)+1;enemy.speed*=1.15;}
         raidAnnounce('BEHEMOTH DOWN · Zombies now have more health and move 15% faster.');
     } else if(z.isLabBoss)raidAnnounce('BEHEMOTH DOWN · Keep moving.');
-    if(Math.random()<(z.isBoss?1:.10))zmPowerups.push({x:z.x,y:z.y,type:['speed','spread','nuke'][Math.floor(Math.random()*3)]});
+    if(!z.isRaider&&Math.random()<(z.isBoss?1:.10))zmPowerups.push({x:z.x,y:z.y,type:['speed','spread','nuke'][Math.floor(Math.random()*3)]});
 }
 function raidBlast(x,y,radius) {
     raidExplosions.push({x,y,r:radius,life:20,maxLife:20});
@@ -2088,9 +2111,15 @@ function drawRaidLoot() {
 function drawRaidEffects() {
     ctx.save();
     for(const fx of raidExplosions){const progress=1-fx.life/fx.maxLife;ctx.globalAlpha=1-progress;ellipse(ctx,fx.x-camera.x,fx.y-camera.y,Math.max(1,fx.r*progress),Math.max(1,fx.r*progress),'#eaaa5a55');ctx.strokeStyle='#ffd18a';ctx.lineWidth=3;ctx.beginPath();ctx.arc(fx.x-camera.x,fx.y-camera.y,Math.max(1,fx.r*progress),0,Math.PI*2);ctx.stroke();}
+    if(raidAircraft){const x=raidAircraft.x-camera.x,y=raidAircraft.y-camera.y;ctx.fillStyle='#263a3c';ctx.fillRect(x-100,y-12,190,24);ctx.fillRect(x-22,y-27,82,54);ctx.fillStyle='#63746d';ctx.fillRect(x-118,y-3,240,6);ctx.fillStyle='#192929';ctx.fillRect(x+48,y-42,7,84);ctx.fillStyle='#dfd19b';ctx.fillRect(x+57,y-8,10,6);}
+    for(const b of raidEnemyBullets){ctx.strokeStyle='#ed8f68';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(b.x-camera.x-b.vx*.7,b.y-camera.y-b.vy*.7);ctx.lineTo(b.x-camera.x,b.y-camera.y);ctx.stroke();}
+    if(raidRadio&&!raidRadio.carried){const x=raidRadio.x-camera.x,y=raidRadio.y-camera.y;ellipse(ctx,x,y+9,12,5,'#071b1a88');ctx.fillStyle='#c8a65c';ctx.fillRect(x-9,y-9,18,17);ctx.fillStyle='#1c2827';ctx.fillRect(x-5,y-5,10,7);ctx.strokeStyle='#e7d183';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(x+5,y-9);ctx.lineTo(x+11,y-17);ctx.stroke();}
+    if(raidSupport){const x=raidSupport.x-camera.x,y=raidSupport.y-camera.y;if(raidSupport.drop>0){const h=28+raidSupport.drop*.7;ctx.strokeStyle='#d6d1b0';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(x,y-h+16);ctx.lineTo(x-14,y-h);ctx.lineTo(x+14,y-h);ctx.closePath();ctx.stroke();ctx.fillStyle='#b8a982';ctx.fillRect(x-4,y-h+15,8,12);ctx.strokeStyle='#aaa58c';ctx.beginPath();ctx.moveTo(x-10,y-h+1);ctx.lineTo(x-4,y-h+16);ctx.moveTo(x+10,y-h+1);ctx.lineTo(x+4,y-h+16);ctx.stroke();}else{const a=raidSupport.target?Math.atan2(raidSupport.target.y-raidSupport.y,raidSupport.target.x-raidSupport.x):0;ctx.save();ctx.translate(x,y);ctx.rotate(a);ellipse(ctx,0,7,14,6,'#071b1a80');ctx.fillStyle='#65736a';ctx.fillRect(-8,-10,16,20);ctx.fillStyle='#293332';ctx.beginPath();ctx.arc(4,0,7,0,Math.PI*2);ctx.fill();ctx.fillStyle='#172222';ctx.fillRect(5,-3,20,5);if(raidSupport.flash>0){polygon(ctx,[[25,0],[34,-3],[32,4]],'#fff0b5');}ctx.restore();}}
     ctx.restore();
     const weapon=document.getElementById('raid-weapon');if(weapon)weapon.textContent=RAID_WEAPONS[raidWeapon].name;
     ctx.fillStyle='#172d2dee';ctx.fillRect(20,100,245,27);ctx.fillStyle=RAID_WEAPONS[raidWeapon].color;ctx.textAlign='left';ctx.font='12px system-ui';ctx.fillText(RAID_WEAPONS[raidWeapon].name.toUpperCase(),34,118);
+    ctx.fillStyle='#172d2dee';ctx.fillRect(20,132,245,24);ctx.fillStyle='#e8d9b1';ctx.font='11px system-ui';ctx.fillText('HEALTH '+Math.max(0,zmPlayer.hp)+' / '+zmPlayer.maxHp+'  ·  BANDAGES '+raidBandages+'  ·  ARMOR '+raidArmor,34,149);
+    if(raidRadio&&raidRadio.carried){ctx.fillStyle='#172d2dee';ctx.fillRect(20,161,245,21);ctx.fillStyle='#e8d9b1';ctx.fillText(raidSupport?'ASSISTANCE ON SITE · SERVICE PISTOL':'RADIO READY · PRESS R FOR ASSISTANCE',34,176);}
     const lab=document.getElementById('raid-lab');if(lab)lab.textContent=!raidLabBroken?Math.min(zmScore,70)+' / 70 kills · Lab sealed':zmZombies.some(z=>z.isLabBoss&&z.hp>0)?zmZombies.filter(z=>z.isLabBoss&&z.hp>0).length+' Behemoth(s) active · Red dots':raidReinforcements===2?'All Behemoths defeated':raidBossDefeated?'Zombies strengthened · Two bosses at 125':'Lab breached · Two more bosses at 125';
     if(zmFrame<raidNoticeUntil){ctx.fillStyle='#172d2dee';ctx.fillRect(20,(canvas.height/DF_TOPDOWN_ZOOM)-83,Math.min((canvas.width/DF_TOPDOWN_ZOOM)-40,510),43);ctx.fillStyle='#ffe0a6';ctx.textAlign='left';ctx.font='bold 12px system-ui';ctx.fillText(raidNotice,30,(canvas.height/DF_TOPDOWN_ZOOM)-57,Math.min((canvas.width/DF_TOPDOWN_ZOOM)-60,490));}
 }
@@ -2165,6 +2194,7 @@ function paintCity() {
     }
 }
 function drawUndead(z) {
+    if(z.isRaider){const a=Math.atan2(zmPlayer.y-z.y,zmPlayer.x-z.x),g=Math.sin(zmFrame*.16+z.phase)*3;ctx.save();ctx.translate(z.x-camera.x,z.y-camera.y);ctx.rotate(a);ellipse(ctx,3,5,22,13,'#061c1a80');ctx.fillStyle='#273b3c';ctx.fillRect(-13,-10,24,20);ctx.fillStyle='#7a8074';ctx.fillRect(4,-9,13,18);ctx.fillStyle='#181f20';ctx.beginPath();ctx.arc(10,0,10,0,Math.PI*2);ctx.fill();ctx.fillStyle='#7f998f';ctx.fillRect(7,-4,8,3);ctx.strokeStyle='#75847d';ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(-7,-7);ctx.lineTo(17+g,-13);ctx.stroke();ctx.fillStyle='#1a2525';ctx.fillRect(15,-15,20,5);ctx.restore();return;}
     const a=Math.atan2(zmPlayer.y-z.y,zmPlayer.x-z.x),s=z.isLabBoss?2.1:z.isBoss?1.65:1;
     const gait=reducedMotion?0:Math.sin(zmFrame*.12+(z.phase||0));
     ctx.save();ctx.translate(z.x-camera.x,z.y-camera.y);ctx.rotate(a);ctx.scale(s,s);
@@ -2192,6 +2222,7 @@ canvas.addEventListener('mousedown',e=>{if(e.button===0&&isPlaying&&!paused&&cur
 window.addEventListener('mouseup',()=>raidFiring=false);window.addEventListener('blur',()=>raidFiring=false);
 document.getElementById('pause-button').addEventListener('click',()=>raidFiring=false);
 window.addEventListener('keydown',e=>{if(e.code==='KeyP'||e.code==='Escape')raidFiring=false;});
+window.addEventListener('keydown',e=>{if(!isPlaying||currentGame!=='zombies'||e.repeat)return;if(e.code==='KeyQ'){if(raidReinforcements===2&&!zmZombies.some(z=>z.isLabBoss&&z.hp>0)){const choices=[...raidUnlockedWeapons],at=choices.indexOf(raidWeapon);raidWeapon=choices[(at+1)%choices.length];raidAnnounce('WEAPON SELECT · '+RAID_WEAPONS[raidWeapon].name);}else raidAnnounce('WEAPON SELECT UNLOCKS AFTER THE TWO BEHEMOTHS.');}if(e.code==='KeyH'&&raidBandages>0&&zmPlayer.hp<zmPlayer.maxHp){raidBandages--;zmPlayer.hp=Math.min(zmPlayer.maxHp,zmPlayer.hp+8);raidAnnounce('BANDAGE USED');}if(e.code==='KeyR'&&raidRadio&&raidRadio.carried&&!raidSupport){raidSupport={x:zmPlayer.x-90,y:zmPlayer.y+75,drop:88,shot:20,target:null,flash:0};raidAnnounce('ASSISTANCE INBOUND · ONE ALLY, ONE SERVICE PISTOL');}});
 if(location.hash==='#raid'){appContainer.style.display='none';helpBar.style.display='none';arcadePortal.style.display='flex';currentGame='zombies';document.querySelectorAll('.game-tab').forEach(t=>t.classList.toggle('active',t.dataset.game==='zombies'));updateSidebarUI('zombies');}
 
 </script>

